import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)


const store = new Vuex.Store({
  state: { // 仓库数据
  },
  mutations: { // 数据有哪些变化
  },
  actions: {
  }
})

window.store = store;

export default store
